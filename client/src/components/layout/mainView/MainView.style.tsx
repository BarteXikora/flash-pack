import styled from 'styled-components'

const StyledMainView = styled.div`
    .main {
        padding: ${p => p.theme.gap.big};
        padding-top: ${p => p.theme.gap.medium};
    }
`

export default StyledMainView